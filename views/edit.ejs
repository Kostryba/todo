<% include partials/header %>

    <div class="container">
    <div class="row">
        <div id="form-note">
            <form action="/<%= note._id %>?_method=PUT" method="POST">
                <div class="form-group">
                    <input class="form-control" type="text" name="title" value="<%= note.title %>">
                    <textarea class="form-control" rows="4"  name="text" ><%= note.text %></textarea>
                    <button class="btn btn-default btn-lg btn-block">EDIT NOTE!</button>
                </div>
            </form>
        </div>
    </div>
 
<% include partials/footer %>